# Personas Directory

Store one persona dossier per historical figure in JSON or YAML form.

Each dossier should map cleanly to `app.models.schemas.PersonaDossier`.

Current sample dossiers:

- `einstein.json`
- `gandhi.json`
- `cleopatra.json`

Keep speech style and evidence separate in spirit even when both appear in the same dossier schema.
